<div class="grid md:grid-cols-2 gap-x-3">
    @if($showBannerDanger)
        <div class="col-span-2 md:col-span-2 fi-banner">
            Antes de registrar tickets debes cerrar la caja pendiente
        </div>
    @else
        Test
    @endif
</div>
